package employee

import hotel.RoomType
import java.time.LocalDate

class BookingService {
    fun book(employeeId: Int, hotelId: Int, roomType: RoomType, checkin: LocalDate, checkout: LocalDate): Booking? {
        return null
    }
}